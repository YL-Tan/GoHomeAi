name: gohomeai
channels:
  - conda-forge
  - pytorch
dependencies:
  # core interpreter
  - python=3.11

  # tooling
  - black
  - isort
  - pre-commit
  - pyrefly
  - jupyterlab

  # data / ML
  - dvc
  - numpy<2
  - pandas
  - scipy
  - scikit-learn
  - matplotlib
  - seaborn
  - pytorch
  - torchvision
  - torchaudio
  - cpuonly   # remove if use CUDA later

  # fastapi runtime
  - fastapi
  - uvicorn[standard]

  - pytest
  - pytest-cov 
  # pip extras, if any
  - pip:
      - transformers
      - sentencepiece